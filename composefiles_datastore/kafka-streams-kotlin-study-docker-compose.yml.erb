version: "3"

services:
  app:
    build:
      context: .
    volumes:
      - .:/usr/src/app:cached
    <%- if ENV["NETWORK"] -%>
    networks:
      - <%= ENV["NETWORK"] %>
    <%- end -%>
<%- if ENV["NETWORK"] -%>
networks:
  <%= ENV["NETWORK"] %>:
    external: true
<%- end -%>
