compose_error_descriptions = {
    1: "No passwords or secrets in the docker compose file.",
    2: "Limit the use of bind mounts, especially for sensitive host directories. Consider using Docker volumes instead, which are managed by Docker and provide better encapsulation and management features.",
    3: "Open/Accessible Port",
    4: "Two images share volume locations in the disk.",
    5: "Weak Permissions",
}