version: "3.8"
services:
  webapp:
    image: webapp:latest
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: postgres:12
    volumes:
      - db-data:/var/lib/postgresql/data
volumes:
  db-data: {}
networks:
  default:
    driver: bridge
